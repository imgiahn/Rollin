<?xml version="1.0" encoding="utf-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.poscoict.rollin.user.repo.UserMapper">
    <select id="getAllUser" resultType="PaperDto">
        SELECT id, userId,
               password,
               name, img, pcnt
        FROM
            users;
    </select>


    <insert id="insertUser" parameterType="PaperDto">
        Insert Into
        users(userId, password, name)
        VALUES
        (#{userId},#{password}, #{name})
    </insert>


</mapper>