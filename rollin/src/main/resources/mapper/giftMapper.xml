<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.poscoict.rollin.gift.repository.GiftMapper">
    <select id="getAllGift" resultType="GiftDto">
        SELECT
            id,price,content, count, name, img, views
        FROM
            gifts
        ORDER BY name
    </select>
    
    <select id="searchGiftByName" resultType="GiftDto" parameterType="GiftDto">
        SELECT
            price,content,name,img
        FROM
            gifts
        WHERE
            name like CONCAT('%', #{name}, '%')
    </select>
    
    <update id="viewCountById" parameterType="GiftDto">
        UPDATE
            gifts
        SET
            views = views + 1
        WHERE
            id=#{id}
    </update>

    <select id="getGiftById" parameterType="Integer" resultType="GiftDto">
        SELECT
            id, price, content, name, img
        FROM
            gifts
        WHERE id=#{id}
    </select>

    <insert id="insertGift" parameterType="PaperDto">
        INSERT INTO
            papers(userId, nickname, content, giftId)
        VALUES
            (#{userId}, #{nickname}, #{content}, #{giftId})
    </insert>

    <update id="updateGiftCount" parameterType="Integer">
        UPDATE gifts
        SET count = count +1
        WHERE id = #{id}
    </update>

    <select id="findReceiverNotUserId" parameterType="Integer" resultType="UserDTO">
        SELECT
            id, name
        FROM
            users
        WHERE id != #{id}
    </select>

</mapper>